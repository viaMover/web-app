import { PictureDescriptor } from '@/components/html5';

export default {
  src: require('./staking-light-theme@1x.png'),
  sources: [{ src: require('./staking-light-theme@2x.png'), variant: '2x' }]
} as PictureDescriptor;
