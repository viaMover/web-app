import { PictureDescriptor } from '@/components/html5';

export default {
  src: require('./info-polygon@1x.png'),
  sources: [{ src: require('./info-polygon@2x.png'), variant: '2x' }]
} as PictureDescriptor;
