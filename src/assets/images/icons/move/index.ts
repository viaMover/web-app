import { PictureDescriptor } from '@/components/html5';

export default {
  src: require('./info-move@1x.png'),
  sources: [{ src: require('./info-move@2x.png'), variant: '2x' }]
} as PictureDescriptor;
