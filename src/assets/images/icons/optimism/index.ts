import { PictureDescriptor } from '@/components/html5';

export default {
  src: require('./info-optimism@1x.png'),
  sources: [{ src: require('./info-optimism@2x.png'), variant: '2x' }]
} as PictureDescriptor;
