import { PictureDescriptor } from '@/components/html5';

export default {
  src: require('./debit-card@1x.png'),
  sources: [{ src: require('./debit-card@2x.png'), variant: '2x' }]
} as PictureDescriptor;
