export default {
  async estimateSwap(
    inputAddress: string,
    outputAddress: string,
    inputAmount: string,
    outputAmount: string
  ): Promise<void> {
    console.log('Estimating swap...');
  }
};
